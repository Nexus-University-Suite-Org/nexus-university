# Frontend URLs
# Points to the API backend. Update when the new Spring Boot backend is live.
VITE_API_BASE_URL=http://localhost:8080
# Points to the NAD Admissions Dashboard backend (admissions-server), source of admission/programme records.
VITE_NAD_API_BASE_URL=http://localhost:8083
